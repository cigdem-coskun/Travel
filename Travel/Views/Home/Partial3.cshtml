@model List<BlogModel>
<div class="padding">
    <h4>En Popüler Bloglar</h4>
    <ul class="list list-pad">
        @{
            int i = 0;
        }
        @foreach (var item in Model)
        {
            
      
        <li class="list-bg"><span>@{i++;}@i.</span><a href="/Blog/Index/@item.Id">@item.Baslik</a></li>
        
          }
    </ul>
    <a href="#" class="link1">See all hotels</a>
</div>